# ポートフォリオサイト（RyuWorks）

**公開URL: https://ryuworks-portfolio.vercel.app**（2026-09-06 公開）
main に push すると Vercel が自動デプロイする。

押田竜太 / RyuWorks のポートフォリオ。1ページ完結。
目的は **「この人に頼めば、うちの面倒な作業が減りそうだ」と3分で思わせること**。
読むのは宮崎の中小企業の経営者・支援機関の担当者・AIコミュニティで会った人。コードは読まない。

## 文面の正本

サイトの文言はすべて `app/content.ts` に集約している。JSXに直接文字を書かないこと。
さらに上流の正本は次の3ファイル。**文面を直すときはまずこちらを直す。**

- `~/Projects/案件応募/2026-08-30_実績原稿9件.md`
- `~/Projects/案件応募/2026-08-30_ヒーロー・プロフィール文案.md`
- `~/Projects/案件応募/2026-08-30_できること4カテゴリ.md`

## 書き方のルール

- どの文も「発注者が得すること」を書く。「使った技術がすごいこと」は書かない
- 実績カードは4行テンプレを厳守（困りごと／作ったもの／結果／使った技術）
- **第1部（liveWorks）は「困っていたこと」、第2部（caseWorks）は「想定した困りごと」。**
  第1部は実際に動いているものなので「想定」と書かない
- 【結果】は本人が対面で根拠を説明できる数字だけを書く。説明できないものは書かない
- **「農協」「JA」「勤務先」は書かない。** 業種としての「畜産」だけ
- **所在地は「宮崎県」まで。** 市町村は書かない
- AI開発を始めた時期・経験年数は書かない（聞かれたら口頭で正直に答える）
- 顔写真は載せない
- **似て見える2組は、技術名ではなく「誰が使うか」「入口はどこか」で書き分ける**
  - A-2（LINE予定登録）と B-3（Web接客）… 入口がLINEかWebか／相手が常連か初見の客か
  - B-1（社内資料の検索AI）と B-2（Slack）… 使う人が調べる担当者か、全社員か
- **画像は4行の説明の「下」に置く。** 先に画像が来ると文章が読まれない

## やらないこと

CMS導入、ブログ機能、多言語対応、凝ったアニメーション、ダークモード対応、別ページの追加。
実績が増えたら `app/content.ts` の配列に足して縦に伸ばす。

## 技術構成

Next.js 16 (App Router) / TypeScript / Tailwind CSS v4 / Vercel。
色は2色まで（生成り `--background` ＋ 深緑 `--accent`）。フォントは Noto Sans JP。
スマホ表示が最優先。地方の経営者はほぼスマホで見る。

## 問い合わせ先の設定

`app/content.ts` の `LINE_URL` / `FORMSPREE_ID`。

- **`PLACEHOLDER` のままのチャンネルは、サイトに表示されない。**
  片方だけ用意できた段階で公開でき、もう片方は後から足せる
- 両方が未設定だと問い合わせ手段がゼロになるので `npm run check` で止まる
- LINE公式アカウントの手順は `~/Projects/案件応募/2026-08-30_LINE公式アカウント開設手順.md`

**公開を遅らせないことを優先する。** 先に埋まった方で公開し、あとから追加する。

## デモリンクの方針

**動いていないものにはリンクを貼らない。** 現在リンクがあるのは B-2（case7-rag-slack）だけ。
**そのリンク先も「触れるデモ」ではなく説明ページ**（実測値・できること・できないことを記載）。
ラベルで「実際に動くものを見る」のように**触れると誤解させる書き方をしない**。

- B-1 社内資料の検索AI … 限定ログインで外部から入れない
- B-3 Webサイトの問い合わせ自動対応 … Supabase停止中。Resumeすれば復活する
- B-4 売上の見える化 … ログイン必須。画面に本人のメールアドレスが出るためスクショも流用しない

## 開発

```bash
npm run dev     # localhost:3000
npm run check   # 問い合わせ先が設定されているか確認
npm run build   # 公開前に必ず通す
```
